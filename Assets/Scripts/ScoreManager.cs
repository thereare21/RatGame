using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScoreManager : MonoBehaviour
{

    public static int ratsKilled;

    // Start is called before the first frame update
    void Start()
    {
        ratsKilled = 0;
    }

    // Update is called once per frame
    void Update()
    {

    }

}
